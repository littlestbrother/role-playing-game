import RockPaperScissors from '../src/rps.js'

// describe('', () => {
//     test('should test', () => {
//         let foo = new bar('bar');
//         expect(foo).toEqual('bar');
//     });
// });

describe('', () => {
    test('should test', () => {
        let foo = new bar('bar');
        expect(foo).toEqual('bar');
    });
});
